syntax = "proto3";

// TODO(burdon): Rename dxos.any (and entire package?)
// TODO(burdon): Why not import "google/protobuf/any.proto"?

package dxos.codec;

message Any {
  string type = 1;
  bytes value = 2;
}
